name: Orders Notify (nounas_food) retired

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Nouna order notifications are handled by orders_notify_fcm_nounas.yml."
